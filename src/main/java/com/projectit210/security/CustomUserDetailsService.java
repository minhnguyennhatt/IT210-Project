package com.projectit210.security;

/**
 * CustomUserDetailsService - không sử dụng (không dùng Spring Security)
 * Xác thực được xử lý bởi JwtFilter + AuthInterceptor
 */
public class CustomUserDetailsService {
}
