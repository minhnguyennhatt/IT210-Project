package com.projectit210.security;

/**
 * SecurityConfig - không sử dụng (không dùng Spring Security starter)
 * Bảo mật được xử lý bởi JwtFilter + AuthInterceptor
 */
public class SecurityConfig {
}
